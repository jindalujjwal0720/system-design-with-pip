interface CommentsAreaProps {
  className?: string;
}

const CommentsArea = ({ className }: CommentsAreaProps) => {
  return <div className={className}>CommentsArea</div>;
};

export default CommentsArea;
